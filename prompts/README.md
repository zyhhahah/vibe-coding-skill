# prompts

这里存放可独立复用的提示词，不依赖某个特定编辑器的 skill 机制。

## 建议写法

每个提示词文件尽量写清楚：

- 用途
- 适用场景
- 输入项
- 输出格式
- 示例

## 建议分法

- `coding/`
- `writing/`
- `product/`
- `research/`

当某个 prompt 已经演变成固定工作流时，考虑把它升级到 `playbooks/` 或 `skills/`。
